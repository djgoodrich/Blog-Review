<div class="container">
    {{> navbar/navbar}}
    <h1 class="mt-3 mb-3" id="userHead">{{blog.title}}</h1>
    <p class="underline"><a href="{{blog.website}}" target="_blank">{{blog.website}}</a></p>
    <p class="indent">Cumulative rating: <span class="stars">{{blog.cumulative_rating}}</span> </p>
    <p class="indent">Description: {{blog.description}}</p>
    <h2>Reviews</h2>
    <button type="button" class="btn btn-warning" data-toggle="modal" 
        data-target={{#unless currentUserSub}}"#requiresLoginModal"{{/unless}}
        {{#if currentUserSub}}"#writeReviewModal"{{/if}}>Write Review
    </button>    
    {{#each blog.Reviews}}
    {{#if this.body}}
    <div class="card mt-3">
        <div class="card-body">
            <h3 class="title">{{this.title}}</h3>
            <h4><em><a class="colored-link" href="/user/{{this.User.id}}">{{this.User.name}}</a></em></h4>
            <p><span class="stars">{{this.rating}}</span> </p>
            <p>{{this.body}}</p>
        </div>
    {{/if}}
    </div>
    {{/each}}
</div>

{{#unless currentUserSub}}
    {{> modals/requiresLogin}}
{{/unless}}

{{#if currentUserSub}}
    {{> modals/writeReview}}
{{/if}}

{{> scripts/scripts}}